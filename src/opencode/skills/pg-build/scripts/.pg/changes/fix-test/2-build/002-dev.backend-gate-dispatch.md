## 任务： - 

### 变更名称
fix-test

### Track 配置
- track.id: 
- track.review_level: 
- track.modules: 
- track.max_fix_retries: 
- track.fix_routing: 

### Module 配置


### Stage 配置
- stage.name: 
- stage.test_key:   # unit / integration / e2e
- stage.gate:   # all_pass / any_pass / no_gate
- stage.environment.required: 
- stage.environment.prepare.status: 
- stage.environment.prepare.log_path: 
- stage.environment.prepare.message: 
- stage.environment.name: 

- stage.test_commands: 

### Gate 审计要求
- `stage.gate` 已写入 Track 配置（见上）。
- **只读不写**源码；**完成后用 `cat >` 自行写盘**到
  `2-build/003--gate-verify.md`，
  不要把 markdown 全文塞进返回里。
- 按 design.md 列 P-N 审计项逐项核对 evidence。

> 关于 seq 编号：dispatch 文件 (`002`) 与本报告
> (`003`) 共享全局递增序列；本报告的 seq 由
> runner 预分配，禁止更改。


### 运行时环境查询

如需在运行时查询 prepare_env 状态，用 runner 子命令（避免硬编码日志路径）：

```bash
python3 .opencode/skills/pg-build/scripts/pg-pipeline-runner.py prepare-env-status fix-test [stage_name]
```

服务启停由 LLM 自行判断时机：runner 不替你启停任何 role 服务。

### 产物路径
- proposal: .pg/changes/fix-test/proposal.md
- design: .pg/changes/fix-test/design.md
- tasks: .pg/changes/fix-test/tasks.md

### 模块路径约束（硬约束）

本 track 只允许修改以下模块根目录：
null
track 名称 `` 拥有模块：，各模块根目录已去重合并。

硬规则：
1. **只能**在  + `.pg/` 下创建/修改文件
2. 写入其他模块目录（如本 track 是 `backend` 时写入 `<other-module-dir>/`）或项目根目录 → 严重违规
3. `real-integration` track（modules=[]）跳过此约束

### 执行要求

执行  阶段，任务如下：



**验证要求**：




### 返回格式

- summary: 一句话总结执行结果
- outputs: 产物文件列表（具体文件名）
- tasks_updated: 是否已更新 tasks.md 复选框（true/false）
- status: SUCCESS / FAILED


