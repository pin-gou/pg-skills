# test.yaml — sub=test 完整提示词

prompt: |
  ### 测试要求

  TDD 红 Phase：本阶段只写测试代码，绝不创建或修改任何生产代码。
  预期首次编译失败（红 phase 属性）。

  - 只创建测试文件
  - 不修改任何生产代码文件
  - 可以修改测试配置文件（如 pom.xml、package.json 的测试依赖）

  ### 模块路径约束

  禁止编辑以下路径的文件：
  - 不在 {module_roots} 路径下的文件